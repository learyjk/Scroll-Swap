{
  "name": "scroll-swap",
  "version": "1.0.0",
  "description": "swap content on scroll",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/index.ts --watch --minify --bundle --outfile=dist/index.js"
  },
  "author": "Web Bae LLC",
  "license": "MIT",
  "dependencies": {
    "gsap": "^3.11.5"
  },
  "devDependencies": {
    "esbuild": "^0.17.18",
    "typescript": "^5.0.4"
  }
}
